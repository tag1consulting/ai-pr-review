# generated: 2026-05-18T11:48:00-07:00
# last_updated: 2026-07-01T15:22:00-07:00
# project: ai-pr-review
# project_key: NOKEY
# tracking_system: file-system
# story_location: memory-bank/bmad/stories

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - SM typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-18T11:48:00-07:00
last_updated: 2026-06-09T02:00:00-07:00
project: ai-pr-review
project_key: NOKEY
tracking_system: file-system
story_location: memory-bank/bmad/stories

development_status:
  # ── Epic 0 — Golden Parity Harness ─────────────────────────────────────────
  # Parent issue #194 CLOSED. Branch: rework/epic-0-harness
  epic-0: done
  0-1-record-mode: done
  0-2-seed-fixture-corpus: done
  0-3-payload-level-diff-harness: done
  0-4-inline-eligibility-oracle: done
  0-5-config-parity-matrix: done
  0-6-parity-ci-workflow: done
  epic-0-retrospective: optional

  # ── Epic 1 — Python Core (Compute Only) ────────────────────────────────────
  # Parent issue #195 CLOSED. Branch: rework/epic-1-python-core
  epic-1: done
  1-1-python-package-scaffold: done
  1-2-engine-switch-for-compute: done
  1-3-typed-config: done
  1-4-diff-computation-and-line-mapping: done
  1-5-language-detection-and-file-manifest: done
  1-6-findings-pipeline: done
  1-7-analyzer-bridge-with-pre-filter: done
  1-8-cve-lockfile-scanning: done
  1-9-pricing-port: done
  1-10-compute-to-posting-json-handoff: done
  epic-1-retrospective: optional

  # ── Epic 2 — LLM, VCS, Dispatch, Posting ───────────────────────────────────
  # Parent issue #196 CLOSED. Branch: rework/epic-2-llm-vcs-dispatch
  # Story files present in memory-bank/bmad/stories/ for all 13 stories.
  epic-2: done
  2-1-llm-client-port: done
  2-2-agent-roster: done
  2-3-agent-dispatch: done
  2-4-conditional-gates: done
  2-5-summarizer: done
  2-6-outcome-classifier: done
  2-7-watermark-policy: done
  2-8-ownership-marker: done
  2-9-vcs-github: done
  2-10-vcs-gitlab: done
  2-11-vcs-bitbucket: done
  2-12-remove-shim: done
  2-13-expand-fixtures: done
  epic-2-retrospective: optional

  # ── Epic 3 — Opt-in Capabilities ───────────────────────────────────────────
  # Parent issue #197 CLOSED. Branch: rework/epic-3-opt-in-capabilities
  epic-3: done
  3-1-treesitter-parsers: done
  3-2-symbol-cross-file-lookup: done
  3-3-context-budget-injection: done
  3-4-context-parity-carveout: done
  3-5-sarif-ingestor: done
  3-6-sarif-example-workflows: done
  3-7-slash-command-parser-handlers: done
  3-8-learning-store: done
  3-9-store-retention: done
  3-10-prompt-injection: done
  3-11-explain-revise-agent-callback: done
  3-12-docs-first-dismissal-privacy-warning: done
  epic-3-retrospective: optional

  # ── Epic 4 — Soak, Observability, Default Flip ─────────────────────────────
  # Parent issue #198 CLOSED. v1.0.0 released 2026-06-02.
  # All issues #246-#250 CLOSED. publish-image.yml run 26853323826 succeeded; v1.0.0 image confirmed pullable.
  epic-4: done
  4-1-structured-logging: done
  4-2-telemetry-hooks: done
  4-3-cost-table-refinement: done
  4-4-perf-tuning-cache-priming: done
  4-5-error-surface-polish: done
  4-6-docs-refresh-python-primary: done
  4-7-field-soak: done
  4-8-deprecation-warning-bash: done
  4-9-flip-default-engine: done
  4-10-release-python-as-default: done
  epic-4-retrospective: optional

  # ── Epic 5 — Delete Bash ────────────────────────────────────────────────────
  # Parent issue #199 CLOSED. PR #525 merged 2026-06-22 at 8ba85f4. v2.0.0 tagged.
  # ~9,600 LOC deleted across 59 shell files. Python is the sole engine.
  epic-5: done
  5-1-analyzer-wrapper-audit: done       # #256 closed obsolete (Epic 8 already covered it)
  5-2-delete-orchestrator-and-libs: done # #251 commit 1e316fc
  5-3-delete-post-scripts: done          # #252 commit a596c82
  5-4-delete-bats-suite: done            # #253 commit 1a5512a
  5-5-entrypoint-simplification: done    # #254 commit 5368b54
  5-6-container-slim-down: done          # #255 commit a1fc912
  5-7-docs-final-cleanup: done           # #257 commit 878c355
  5-8-final-release: done                # #258 tagged v2.0.0 at 8ba85f4; release live
  epic-5-retrospective: optional

  # ── Epic 6 — Python-Engine Hardening ────────────────────────────────────────
  # Parent issue #361. #356/#357/#358 all CLOSED. One story remains.
  # #360 (partial): provenance weighting — deterministic confidence boost when
  # static analyzer + LLM agent agree on the same file+line region.
  # Branch: feat/epic6-provenance-weighting
  epic-6: done
  6-1-provenance-weighting: done            # #360 — merged PR #528 at 2026-06-22
  epic-6-retrospective: optional

  # ── Epic 8 — Port Analyzer Wrappers to Native Python ────────────────────────
  # Parent issue #461 OPEN. Milestone: Epic 8 (milestone/9)
  # Batch 1 (LOW): shellcheck, ruff, hadolint — establish native-analyzer pattern
  # Batch 2 (LOW-MED): kube-linter, phpcs
  # Batch 3 (MED): semgrep, golangci-lint, checkov, phpstan, eslint, tflint
  # Batch 4 (HIGH): trufflehog, cve-check
  epic-8: done
  8-1-port-shellcheck: done
  8-2-port-ruff: done
  8-3-port-hadolint: done
  8-4-port-kube-linter: done
  8-5-port-phpcs: done
  8-6-port-semgrep: done
  8-7-port-golangci-lint: done
  8-8-port-checkov: done
  8-9-port-phpstan: done
  8-10-port-eslint: done
  8-11-port-tflint: done
  8-12-port-trufflehog: done
  8-13-port-cve-check: done
  epic-8-retrospective: optional

  # ── Epic 7 — Review-Quality & Perf Research ─────────────────────────────────
  # Parent issue #362. Issues #353/#354/#358/#359 all CLOSED.
  # Three stories remain: #369 (security prompt audit), #355 (profile routing), #360 remainder (judge pass).
  # Branch: rework/epic-7-quality-research
  epic-7: done
  7-1-security-prompt-audit: done               # #369
  7-2-profile-routing: done                     # #355
  7-3-judge-pass: done                          # #360 remainder — depends on corroborated flag (already on main via PR #528)
  epic-7-retrospective: optional

  # ── Epic 9 — Security Hardening ─────────────────────────────────────────────
  # Standalone security issues not covered by earlier epics.
  # Issue #494: CI canary test enforcing never-execute-checked-out-code invariant.
  epic-9: in-progress
  9-1-canary-security-test: done                # #494 — merged PR #533 at 923c3bd
  epic-9-retrospective: optional

  # ── Epic 10 — VCS Refactor ───────────────────────────────────────────────────
  # GitHub issue #498: extract shared partition/ood-split logic from per-provider
  # post_findings implementations so bug fixes only need to be applied once.
  epic-10: done
  10-1-vcs-partition-dedup: done                  # #498 — PR #542 merged at 7c3d33f
  epic-10-retrospective: optional

  # ── Epic 11 — CLI Decomposition ──────────────────────────────────────────────
  # GitHub issue #497: _run_review_async does too much; _emit_telemetry duplicated
  # at three call sites. Extract preflight + reporting modules; collapse telemetry.
  epic-11: done
  11-1-cli-refactor: done                         # #497 — PR #543 merged at 5d084d2
  epic-11-retrospective: optional

  # ── Epic 12 — Container Maintenance ──────────────────────────────────────────
  # GitHub issue #349: bump container base from ubuntu:24.04 (Python 3.12) to
  # ubuntu:26.04 (Python 3.14) now that soak has long since ended.
  epic-12: done
  12-1-ubuntu26-python314-bump: done              # #349 — PR #544 merged 2026-06-25
  epic-12-retrospective: optional

  # ── Epic 13 — Port Slash-Command Dismiss Orchestration to Python ────────────
  # GitHub issue #554: slash-commands.yml implements dismiss/false-positive/
  # wont-fix entirely in untested inline bash. Four independent bugs found in
  # this code during #550's fix (PR #553) and its live-e2e verification (#555)
  # motivate porting the orchestration into ai_pr_review/, testable via pytest.
  # Plan: memory-bank/bmad/planning-artifacts/ (see approved plan referenced
  # in story 13-1). Phased, one PR per story; story 13-1 ships zero workflow
  # change (module + primitives + tests only).
  # All 5 stories merged 2026-07-03. Issue #554 (core porting) closed
  # manually (auto-close didn't fire on PR #566); issue #562 (13-5 fix)
  # auto-closed correctly on PR #568's merge. Epic complete. A combined
  # :dev pre-tag live-e2e pass spanning 13-2/13-3/13-4/13-5's deferred
  # smoke scope remains tracked separately, not blocking this epic.
  epic-13: done
  13-1-dismiss-module-and-primitives: done        # #554 phase 1 — PR #556 merged at 7adb075; all 11 ACs met, 1706 tests pass, mypy/ruff clean; story at memory-bank/bmad/implementation-artifacts/13-1-dismiss-module-and-primitives.md
  13-2-wire-dismiss-body-finding: done             # #554 phase 2 — PR #558 merged at f3fbabf; new `ai-pr-review dismiss` CLI subcommand; dismiss-body-finding job converted to container: + CLI call; live-e2e smoke partial (CLI substance verified via docker run against real GitHub API; full comment-flow + explain/revise deferred to :dev pre-tag); story at memory-bank/bmad/implementation-artifacts/13-2-wire-dismiss-body-finding.md
  13-3-wire-dismiss-finding: done                  # #554 phase 3 — PR #563 merged at 963a84c; new `ai-pr-review dismiss-inline` CLI subcommand; dismiss-finding job converted to container: + CLI call; also fixed a silent-failure gap shared with 13-2 (DismissResult.errors now surfaced); issue #562 filed for a related shared-helper gap (review-state check before dismiss PUT); live-e2e smoke partial, full comment-flow deferred to :dev pre-tag; story at memory-bank/bmad/implementation-artifacts/13-3-wire-dismiss-finding.md
  13-4-wire-feedback-command: done                 # #554 phase 4 (final) — PR #566 merged at 485c4cb; new `ai-pr-review feedback-context`/`resolve-thread` CLI subcommands; feedback-command's context-extraction + resolve-only steps converted to container: + CLI calls; resolve-thread deliberately never dismisses (matches bash exactly); fixed a $GITHUB_OUTPUT newline-injection risk and two workflow-wiring bugs (pr-number fallback, token privilege) via pre-merge review passes; story at memory-bank/bmad/implementation-artifacts/13-4-wire-feedback-command.md
  13-5-verify-review-state-before-dismiss: done    # #562 follow-up — PR #568 merged at baf78cc; GitHubProvider.get_review_state() + a state check in _dismiss_if_all_resolved(), fail-closed on fetch error, silent skip on non-CHANGES_REQUESTED; live verification (AC10) deferred to the combined :dev pre-tag pass; story at memory-bank/bmad/implementation-artifacts/13-5-verify-review-state-before-dismiss.md
  epic-13-retrospective: optional

  # ── Epic 14 — Model default maintenance ─────────────────────────────────────
  # Single-story maintenance change, tracked without a full PRD/architecture
  # cascade per explicit scope decision: upgrade the default Anthropic
  # "standard" model from Sonnet 4.6 to Sonnet 5. Confirmed two load-bearing
  # fixes beyond the model-ID swap: (1) resolve_temperature()'s reject-list
  # in ai_pr_review/llm/_config.py did not include Sonnet 5, which
  # unconditionally rejects non-default temperature — would 400 every
  # Anthropic call; (2) config/model-pricing.json had no pattern matching
  # claude-sonnet-5, which would silently render "n/a" in the token-cost
  # table. Both fixed; regression tests added for both failure modes.
  epic-14: done
  14-1-sonnet-5-default: done                      # config.py standard-model default, resolve_temperature() reject-list, model-pricing.json entry, docs, new regression tests; story at memory-bank/bmad/stories/14-1-sonnet-5-default.md
  epic-14-retrospective: optional

  # ── Epic 15 — Category-field follow-ups ─────────────────────────────────────
  # GitHub issues #578 (wire category into merge/dedup) and #579 (category
  # coverage for the 13 native static analyzers), both flagged as explicit
  # follow-up scope in #575/#577's PR body when the category field first
  # shipped. Sequenced 15-1 before 15-2: the "other" wildcard dedup rule in
  # 15-1 must exist before 15-2 gives analyzers real categories, or
  # analyzer+agent corroboration can silently break. #579's enum gap (3 of 13
  # analyzers don't cleanly fit the existing 11-value taxonomy) resolved by
  # approximating onto authz/lint rather than adding a 12th enum value, to
  # avoid cross-repo drift with claude-comprehensive-review#76.
  epic-15: done
  15-1-category-dedup: done                        # #578 — category as a merge/dedup clustering signal; story at memory-bank/bmad/stories/15-1-category-dedup.md
  15-2-analyzer-category-coverage: done            # #579 — category assignment across all 13 native analyzers; story at memory-bank/bmad/stories/15-2-analyzer-category-coverage.md
  epic-15-retrospective: optional

  # ── Epic 16 — Semgrep category-mapping hardening ────────────────────────────
  # GitHub issue #585, filed as a deliberate follow-up during #578/#579's PR
  # #584 review: semgrep.py's _map_category() used bare substring containment
  # against check_id, so a rule ID like "python.lang.sqlite-config" matched
  # the "sqli" fragment (intended for sql-injection rules) and was mis-tagged
  # "injection". Non-regressive at the time (every semgrep finding was
  # "other" before #579 shipped _map_category), deferred out of PR #584 to
  # avoid scope creep on that PR's actual fix.
  epic-16: done
  16-1-semgrep-category-hardening: done             # #585 — delimiter-aware anchoring for _CHECK_ID_CATEGORY_HINTS, secret/authz/security-to-other test coverage, check_id-vs-metadata precedence test; story at memory-bank/bmad/stories/16-1-semgrep-category-hardening.md
  epic-16-retrospective: optional
