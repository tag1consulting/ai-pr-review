diff --git a/src/webhooks/parse.ts b/src/webhooks/parse.ts
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/src/webhooks/parse.ts
@@ -0,0 +1,9 @@
+export function extractUserId(payload: unknown): string {
+  const data = payload as any;
+  return data.user.id;
+}
+
+export function isEmptyCount(count: number | string): boolean {
+  return count == 0;
+}
