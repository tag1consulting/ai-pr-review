.claude/
claude/
.serena/
# BMAD planning artifacts (issue #805): untracked, not deleted -- these stay
# on disk locally but no longer live in the product repo's git history going
# forward. Supersedes the two narrower entries this replaced
# (memory-bank/.obsidian/, memory-bank/bmad/soak-log.md).
memory-bank/
_bmad/
_site/
.jekyll-cache/
.jekyll-metadata
docs/Gemfile.lock
.venv-golden/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
pyrightconfig.json

# Local e2e harness run output (result.json, summary.md, container logs,
# telemetry) -- the default --out-dir for tests/e2e/run_e2e.py's `run`
# subcommand. Never intended to be committed; discovered missing during
# this harness's first live run.
tests/e2e/.runs/
